#pragma once

// Generated by CMake from cmake/TransportConfig.hpp.in -- do not edit.
//
// Records which transport backends this build actually contains. The factory uses these to
// decide what makeTransport() can produce, and -- just as importantly -- to produce a
// diagnostic naming the CMake option to turn on when it cannot produce what was asked for.
//
// A build that silently lacks a backend the operator selected is the same silent-degradation
// failure the capability model exists to prevent.

#cmakedefine MOTUS_WITH_AMQPCPP
#cmakedefine MOTUS_WITH_INMEMORY
#cmakedefine MOTUS_WITH_SIMPLEAMQP
